}
.zen-nav-btn:hover { color: #fff; }
.zen-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}
.zen-hero {
  font-size: 3.5rem;
  font-weight: 600;
  line-height: 1.1;
  letter-spacing: -0.03em;
  margin-bottom: 1.5rem;
  color: #f5f5f7;
}
.zen-hero-muted { color: #86868b; }
.zen-subtitle {
  font-size: 1.25rem;
  color: #86868b;
  font-weight: 400;
  margin-bottom: 4rem;
  max-width: 600px;
  line-height: 1.4;
}
.zen-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 24px;
  padding: 2.5rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  transition: transform 0.3s ease;
}
.zen-card:hover {
  transform: translateY(-4px);
  background: rgba(255, 255, 255, 0.05);
}
.zen-card-title {
  font-size: 1.5rem;
  font-weight: 600;
  letter-spacing: -0.02em;
}
.zen-card-meta {
  font-size: 1rem;
  color: #86868b;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.zen-action-btn {
  background: #fff;
  color: #000;
  border: none;
  border-radius: 980px;
  padding: 1rem 2rem;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  align-self: flex-start;
  transition: transform 0.2s, background 0.2s;
}
.zen-action-btn:hover {
  transform: scale(1.02);
  background: #f5f5f7;
}
.zen-action-btn:active {
  transform: scale(0.98);
}
.zen-secondary-metrics {
  margin-top: 6rem;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2rem;
  border-top: 1px solid rgba(255,255,255,0.05);
  padding-top: 3rem;
}
.zen-metric {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.zen-metric-label {
  font-size: 0.85rem;
  color: #86868b;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  font-weight: 600;
}
.zen-metric-value {
  font-size: 2rem;
  font-weight: 600;
  letter-spacing: -0.02em;
}
